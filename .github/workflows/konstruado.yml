name: Konstruas

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2 
      with:
         submodules: 'true'
    - name: Konstruas cxion    
      run: |
        make per-docker
    - uses: actions/upload-artifact@v2
      with:
       name: artfarajxoj
       path: |
        *.pdf
        *.epub
